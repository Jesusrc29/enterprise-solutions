spring:
  jpa:
    show-sql: false
  datasource:
    hikari:
      maximum-pool-size: 20
      minimum-idle: 4
      connection-timeout: 30000

logging:
  level:
    root: INFO
    org.springframework: INFO
    org.flywaydb: INFO
    com.jesusromero.enterprise.employee: INFO
